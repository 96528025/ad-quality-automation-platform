# Test Cases

This document maps product-quality risks to executable test cases. `Actual Result` and `Status` reflect the latest local automated test run.

| Test Case ID | Module | Priority | Preconditions | Test Data | Steps | Expected Result | Actual Result | Status | Automation Reference |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TC-001 | Campaign API | High | API service is available; test database is clean | Campaign payload with `daily_budget = -100` | 1. Send `POST /campaigns` with negative budget | API rejects invalid campaign input with validation error | API returned `422` validation error | Pass | `backend/tests/api/test_campaign_api.py::test_create_campaign_rejects_negative_budget` |
| TC-002 | Ad Delivery | High | Two active campaigns and ads exist; user matches both campaigns | Low-bid campaign and high-bid campaign | 1. Create matching user 2. Create two eligible campaigns 3. Request ad with `POST /ads/request` | Eligible ad with highest delivery score is returned | API returned expected campaign | Pass | `backend/tests/api/test_campaign_api.py::test_request_ad_returns_highest_bid_eligible_campaign` |
| TC-003 | Campaign Eligibility | High | Campaign object exists in test context | Campaign with `status = paused` | 1. Evaluate campaign eligibility | Paused campaign is not eligible for delivery | Eligibility rule excluded paused campaign | Pass | `backend/tests/unit/test_delivery_rules.py::test_paused_campaign_is_not_eligible` |
| TC-004 | Click Tracking | High | No impression exists for provided ID | `impression_id = 999` | 1. Send `POST /events/click` with missing impression ID | API rejects click because impression does not exist | API returned `400` with `impression does not exist` | Pass | `backend/tests/integration/test_ad_quality_workflow.py::test_invalid_click_is_rejected` |
| TC-005 | Conversion Attribution | High | Valid click exists inside attribution window | Valid `click_id`, `conversion_value = 79.99` | 1. Create user/campaign/ad 2. Request ad 3. Record click 4. Send `POST /events/conversion` | Conversion is accepted and linked to click | API returned `201`; metrics included conversion | Pass | `backend/tests/integration/test_ad_quality_workflow.py::test_full_delivery_click_conversion_metrics_workflow` |
| TC-006 | Conversion Attribution | High | Valid click exists | Conversion timestamp 8 days after click | 1. Create click 2. Send conversion with timestamp outside 7-day window | API rejects conversion outside attribution window | API returned `400` with attribution-window error | Pass | `backend/tests/integration/test_ad_quality_workflow.py::test_conversion_outside_attribution_window_is_rejected` |
| TC-007 | Quality Alerts | Medium | Campaign has abnormal CTR | One impression and one click | 1. Create campaign flow 2. Record click 3. Run `POST /quality/campaigns/{id}/run` | High CTR quality alert is created with `open` status | API returned `high_ctr` alert with `open` status | Pass | `backend/tests/integration/test_ad_quality_workflow.py::test_quality_check_creates_high_ctr_alert` |
| TC-008 | Quality Alerts | Medium | Campaign has repeated clicks from same user/ad | Four clicks on same impression | 1. Create campaign flow 2. Record repeated clicks 3. Run quality check | Duplicate click pattern alert is created | API returned `duplicate_click_pattern` alert | Pass | `backend/tests/integration/test_ad_quality_workflow.py::test_quality_check_creates_duplicate_click_pattern_alert` |
| TC-009 | Targeting | High | Campaign targets US users | User country is `CA`; campaign target country is `US` | 1. Create non-matching user 2. Create US campaign/ad 3. Request ad | No eligible ad is returned | API returned `404` with `no eligible ad found` | Pass | `backend/tests/api/test_campaign_api.py::test_request_ad_rejects_country_targeting_mismatch` |
| TC-010 | Targeting | High | Campaign targets ages 18-35 | User age is `17` | 1. Create under-age user 2. Create campaign/ad 3. Request ad | No eligible ad is returned | API returned `404` with `no eligible ad found` | Pass | `backend/tests/api/test_campaign_api.py::test_request_ad_rejects_age_targeting_mismatch` |
| TC-011 | Event Consistency | High | No impression exists for provided ID | `impression_id = 999` | 1. Send click event with invalid impression ID | API rejects click | API returned `400` | Pass | `backend/tests/integration/test_ad_quality_workflow.py::test_invalid_click_is_rejected` |
| TC-012 | Conversion Deduplication | High | One click already has a conversion | Same `click_id` submitted twice | 1. Record first conversion 2. Submit duplicate conversion for same click | Second conversion is rejected | API returned `400` with duplicate conversion error | Pass | `backend/tests/integration/test_ad_quality_workflow.py::test_duplicate_conversion_is_rejected` |
| TC-013 | Service Health | Medium | API service is running | `GET /health` | 1. Call `/health` | API returns service OK | API returned `{"status": "ok"}` | Pass | `backend/tests/integration/test_ad_quality_workflow.py::test_health_and_readiness_checks` |
| TC-014 | Service Readiness | Medium | API service is running; database connection is available | `GET /ready` | 1. Call `/ready` | API verifies database connectivity | API returned `{"status": "ready"}` | Pass | `backend/tests/integration/test_ad_quality_workflow.py::test_health_and_readiness_checks` |
| TC-015 | Alert Lifecycle | Medium | Quality alert exists | Status `investigating`, owner `qa-engineer`, resolution note | 1. Create quality alert 2. Send `PATCH /quality/alerts/{id}` | Alert lifecycle fields are persisted | API returned updated status, owner, and note | Pass | `backend/tests/integration/test_ad_quality_workflow.py::test_alert_lifecycle_update` |
| TC-016 | Metrics Aggregation | High | Impression, click, and conversion have been recorded | One impression, one click, one conversion | 1. Execute full workflow 2. Call `GET /metrics/campaigns/{id}` | Metrics are read from hourly aggregate data and match events | Metrics returned impressions `1`, clicks `1`, conversions `1`, spend `1.2` | Pass | `backend/tests/integration/test_ad_quality_workflow.py::test_full_delivery_click_conversion_metrics_workflow` |
| TC-017 | Event Pipeline | High | Campaign, ad, and user exist | Raw impression event payload | 1. Send `POST /events/ingest` 2. Run `POST /events/process-pending` 3. Query metrics | Raw event starts pending and is materialized into impression metrics | Processor returned `processed = 1`; metrics showed one impression | Pass | `backend/tests/integration/test_event_pipeline.py::test_ingested_impression_event_is_processed_into_metrics` |
| TC-018 | Event Pipeline | High | Processed impression exists | Raw click and conversion event payloads | 1. Ingest click event 2. Process pending events 3. Ingest conversion event 4. Process pending events 5. Query metrics | Processor materializes click and conversion and updates metrics | Metrics showed one impression, one click, one conversion, spend `1.2` | Pass | `backend/tests/integration/test_event_pipeline.py::test_ingested_click_and_conversion_events_are_materialized` |
| TC-019 | Event Pipeline | High | No impression exists for provided ID | Raw click event with `impression_id = 999` | 1. Send `POST /events/ingest` 2. Run `POST /events/process-pending` | Processor marks event as failed and does not materialize click | Processor returned `failed = 1` | Pass | `backend/tests/integration/test_event_pipeline.py::test_invalid_ingested_click_event_is_marked_failed` |
| TC-020 | Ad Review | High | Campaign and user match delivery rules | Ad with `review_status = pending` | 1. Create matching user/campaign 2. Create pending ad 3. Request ad | Pending ad is not delivered | API returned `404` with no eligible ad | Pass | `backend/tests/api/test_campaign_api.py::test_request_ad_rejects_unapproved_ad` |
| TC-021 | Frequency Capping | Medium | Campaign frequency cap is `1` per user per day | Same user requests same campaign twice | 1. Request ad once 2. Request ad again with same user | First request succeeds; second request is blocked by frequency cap | First request returned `200`; second returned `404` | Pass | `backend/tests/api/test_campaign_api.py::test_frequency_cap_blocks_overexposure` |
| TC-022 | Device Targeting | Medium | Campaign targets iOS users | User device is `android` | 1. Create Android user 2. Create iOS-targeted campaign/ad 3. Request ad | No eligible ad is returned | API returned `404` | Pass | `backend/tests/api/test_campaign_api.py::test_request_ad_rejects_device_targeting_mismatch` |
| TC-023 | Interest Targeting | Medium | Campaign targets sports/fitness interests | User interest is `cooking` | 1. Create user with non-matching interest 2. Create campaign/ad 3. Request ad | No eligible ad is returned | API returned `404` | Pass | `backend/tests/api/test_campaign_api.py::test_request_ad_rejects_interest_targeting_mismatch` |
| TC-024 | Ranking | Medium | Two eligible ads exist | High-bid low-quality ad; lower-bid high-quality ad | 1. Create both ads 2. Request ad | Ad with higher effective score wins, not necessarily highest bid | API returned lower-bid high-quality campaign with score `0.18` | Pass | `backend/tests/api/test_campaign_api.py::test_request_ad_uses_effective_ranking_score_not_bid_only` |
| TC-025 | Budget Pacing | Medium | Campaign has pacing enabled | Campaign with large bid at early daily pacing stage | 1. Create paced campaign/ad 2. Request ad | Campaign is blocked when spend would exceed pacing allowance | API returned `404` | Pass | `backend/tests/api/test_campaign_api.py::test_budget_pacing_blocks_overspend_ahead_of_schedule` |
| TC-026 | Invalid Traffic | High | Existing impression has repeated valid clicks; same IP has high recent click volume | Raw click event with same IP and missing user-agent | 1. Create campaign flow 2. Create repeated clicks 3. Ingest high-risk click event 4. Process pending events 5. Query event, metrics, and alerts | Event is marked failed/invalid, click is not materialized, spend and metrics are not increased, invalid-traffic alert is created | Event returned `failed`, `is_invalid = true`, risk score >= 70, metrics click count unchanged, alert created | Pass | `backend/tests/integration/test_event_pipeline.py::test_high_risk_click_event_is_filtered_and_alerted` |
